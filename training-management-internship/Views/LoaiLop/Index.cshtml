@model IEnumerable<training_management_internship.Models.LoaiLop>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenLoaiLop)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenLoaiLop)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LoaiLopId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LoaiLopId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LoaiLopId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
